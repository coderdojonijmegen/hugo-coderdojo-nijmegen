name: Publish Site

on:
  push:
    branches:
      - master

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        name: Checkout Repo

      - uses: actions/checkout@v2
        name: Checkout python-boot-race
        with:
          repository: coderdojonijmegen/python-boot-race
          path: content/instructies/python-boot-race

      - uses: bashopman/hugo-gh-pages@master
        name: Publish Site
        with:
          githubToken: ${{ secrets.PAT }}
          hugoVersion: extended_0.64.1
          args: --gc --minify --cleanDestinationDir
          cname: coderdojonijmegen.familiehopman.net
